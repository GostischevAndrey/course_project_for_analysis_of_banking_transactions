# Проект 1. Приложение для анализа банковских операций

## Описание проекта
Приложение для анализа транзакций, которые находятся в Excel-файле. 
Приложение будет генерировать JSON-данные для веб-страниц, формировать Excel-отчеты, 
а также предоставлять другие сервисы.
## Зависимости проекта:
* python = "^3.13"
* pandas = "^2.2.3"
* requests = "^2.32.3"
* logging-config = "^1.0.4"
* flake8 = "^7.1.1"
* black = "^24.10.0"
* isort = "^5.13.2"
* mypy = "^1.14.1"
## Модули
1. `config.py` - конфигурация
2. `reports.py` - отчеты
3. `services.py` - сервисы
4. `utils.py` - утилиты
5. `views.py` - веб-страница 
6. `main.py` - запуск приложения
## Функции, которые мы будем использовать:
*  `setup_logger` - Настройка логгера с указанным именем и файлом
*  `spending_by_weekday` - Возвращает средние траты в каждом из дней недели за последние три месяца от переданной даты
*  `most_profit_cashback` - Возвращает повышенные категории кэшбэка из списка транзакций за определенные месяц и год
*  `invest_bank` - Возвращает сумму денежных средств, которую можно отложить с трат при заданном размере округления
*  `get_excel_df` - Считывает данные из внешнего файла Excel и возвращает их в формате DataFrame
за период с начала месяца до заданной даты в формате YYYY-MM-DD HH:MM:SS
*  `top_5_operations` - Возвращает 5 самых крупных операции по столбцу 'Сумма операции'
*  `common_information` - Возвращает общую информацию по всем транзакциям
*  `greetings` - Возвращает приветствие пользователю в зависимости от текущего времени суток
*  `get_exchange_rate` - Получает JSON-файл с кодировкой иностранных валют и возвращает значения текущих котировок
*  `get_stock_price` - Получает JSON-файл с наименованиями ценных бумаг и возвращает их текущие котировки
*  `get_monthly_transactions_info` - Принимает дату в формате строки YYYY-MM-DD HH:MM:SS 
и возвращает общую информацию в формате JSON о банковских транзакциях за период с начала месяца до этой даты
## Инструкция по установке
1. Чтобы скачать репозиторий:
```commandline
https://github.com/GostischevAndrey/course_project_for_analysis_of_banking_transactions
```
2. Установите зависимости
```commandline
poetry shell
```
## Тестирование 
Проект протестирован Pytest

## Логирование
1. Файлы логов записываются в папку logs
2. Для модуля reports.py - reports.log
3. Для модуля services.py - services.log
4. Для модуля utils.py - utils.log
5. Файлы логов дополняются

Чтобы запустить тесты с оценкой покрытия выполните команду
```commandline
poetry run pytest --cov
```
Чтобы сгенерировать отчет о покрытии в HTML-формате, где src — пакет c модулями, которые тестируем. 
Отчет будет сгенерирован в папке htmlcov и храниться в файле с названием index.html
.
```commandline
pytest --cov=src --cov-report=html
```
## Команда проекта:
* Гостищев Андрей — Back-End developer
## Контакт для связи с командой разработки:
* pise4kan8@gmail.com
## Источники
Программа создана при поддержке онлайн-школы - SkyPro - skypro@skyeng.ru